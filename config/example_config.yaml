env: "local" # local, dev, prod
http_server:
  address: "localhost:8080"
  timeout: 4s
  idle_timeout: 60s
database:
  user: "postgres"
  password: "postgres"
  host: "localhost"
  port: 5432
  db_name: "dbname"

cache_strategy: "redis" # redis/none

redis:
  url: "localhost:6379"
  password: ""
  db: 0
